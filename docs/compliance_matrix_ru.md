# Матрица соответствия «задание – критерий – реализация»

Формат таблицы соответствует шаблону из `criteria.md` (раздел 5).

| ID задания | ID критерия | Проблема/риск | Где реализовано | Как проверяется | Какие данные нужны | Статус |
|---|---|---|---|---|---|---|
| `B1.1` | `B1-1` | Без явных предусловий запуска жюри не может корректно поднять сервис. | `docs/runbook_ru.md` (разделы 1, 1.1) | Просмотреть runbook, сопоставить требуемые зависимости/переменные с фактическим запуском `docker compose up` и локальным запуском. | Локальная машина/окружение с установленным Python, Docker, доступом к PostgreSQL и Redis. | `done` |
| `B1.2` | `B1-2` | Отсутствие точных команд запуска приводит к неуспешному старту или ручным шагам. | `docs/runbook_ru.md` (разделы 2–5), `entrypoint.sh`, `docker-compose.yml` | По инструкции выполнить миграции и запустить API (`uv run granian ...` или `docker compose up --build`), убедиться в доступности `/health`. | Окружение по предусловиям, переменные окружения по `.env.example`. | `done` |
| `B1.3` | `B1-5` | Без сквозного happy-path нельзя подтвердить работоспособность всей цепочки decide → events → report. | `docs/demo_scenarios_ru.md` (раздел 2.1), `tests/e2e/test_decide_event_report.py` | Пройти сценарий: создание флага/событий/метрики/эксперимента, запуск, /decide, /events, /experiments/{id}/report; сверить ожидаемые ответы. | Набор тестовых сущностей (флаг `e2e_button_color`, типы событий, метрика `e2e_conversion_rate`, эксперимент, субъекты `e2e-user-alice`, `e2e-user-bob`). | `done` |
| `B2.1` | `B2-1` | При отсутствии активного эксперимента система должна возвращать `default`, иначе ломается контрольный сценарий. | `src/application/usecases/decide.py` (`execute`), доменная логика в `DecisionEngine` | Вызвать `POST /decide` для флага без активного эксперимента (см. `docs/demo_scenarios_ru.md` §4.1 и `tests/e2e/test_decide_event_report.py::test_decide_returns_default_without_experiment`). | Флаг `e2e_no_exp_flag` с `default_value`, субъект `anon-user`. | `done` |
| `B2.2` | `B2-3/B2-4` | При наличии применимого эксперимента и таргетинга субъект должен стабильно получать вариант, иначе метрики и UX искажаются. | `src/application/usecases/decide.py`, `src/domain/services/decision_engine.py`, `src/domain/services/participation_guard.py` | Создать и запустить эксперимент на флаге, сделать несколько вызовов `/decide` для одного субъекта, убедиться в детерминизме и корректной выдаче варианта (см. `docs/demo_scenarios_ru.md` §2.2). | Эксперимент `E2E Button Color Test`, субъекты `e2e-user-alice`, `e2e-user-bob`. | `done` |
| `B3.1` | `B3-1/B3-2/B3-3` | Без управляемого жизненного цикла и ревью эксперимент может быть запущен без одобрения. | `src/domain/aggregates/experiment.py` (статусы и переходы), `src/application/usecases/experiment/*`, `src/presentation/rest/routes/experiments.py` | Прогнать сценарий: `draft → in_review → approved → running`, затем попытаться запустить без достаточного числа одобрений (см. `tests/e2e/test_experiments_lifecycle.py`, `docs/demo_scenarios_ru.md` §2.1). | Эксперимент с владельцем-Experimenter и настроенными правилами ревью, админский токен. | `done` |
| `B4.1` | `B4-1/B4-2` | Принятие событий с некорректным типом/структурой ломает аналитику и отчёты. | `src/application/usecases/events/send.py` (`SendEventsUseCase`), валидатор событий; каталог типов событий в `src/application/usecases/event_type/*` | Отправить события с неизвестным `event_type_key` и с неверным типом `timestamp` (см. `docs/demo_scenarios_ru.md` §3.1–3.2, `tests/e2e/test_decide_event_report.py::test_event_unknown_type_rejected`, `::test_events_batch_partial_reject`). | Решение (`decision_id`) для валидного эксперимента, JSON-пэйлоды с ошибками. | `done` |
| `B4.2` | `B4-3` | Дубликаты событий не должны учитываться повторно, иначе метрики искажаются. | `src/application/usecases/events/send.py` (дедупликация), репозиторий событий | Отправить один и тот же event дважды (см. `docs/demo_scenarios_ru.md` §3.3, `tests/e2e/test_decide_event_report.py::test_event_deduplication`), убедиться что `duplicates` растёт, а `accepted` при втором вызове = 0. | Любой валидный `decision_id` для события `exposure`. | `done` |
| `B4.3` | `B4-4/B4-5` | Конверсии без факта показа не должны учитываться, иначе отчёт становится недостоверным. | Доменная атрибуция в `src/domain/services/event_attribution.py`, использование `decision_id` в `SendEventsUseCase` и `GetExperimentReportUseCase` | В e2e-сценарии отправить `e2e_conversion` только после `exposure` с тем же `decision_id` и проверить, что без пары отчёт не учитывает событие (см. `tests/e2e/test_decide_event_report.py`, `tests/e2e/test_reports.py`). | Эксперимент с метрикой `e2e_conversion_rate`, пары событий `exposure`/`e2e_conversion`. | `done` |
| `B5.1` | `B5-1/B5-2/B5-3/B5-4/B5-5` | Без guardrails эксперимент может ухудшать стабильность продакшена незаметно. | `src/domain/entities/guardrail_config.py`, `src/application/usecases/guardrails/check_guardrails.py`, `src/infra/workers/guardrail_checker_worker.py`, репозиторий триггеров | Настроить guardrail у эксперимента, сгенерировать деградацию метрики, дождаться срабатывания воркера; проверить запись в хранилище триггеров и изменение статуса эксперимента, а также структурированный лог `event="guardrail_triggered"` (см. `tests/unit/usecases/test_check_guardrails.py`, `test_check_guardrails_with_notifications.py`). | Эксперимент с guardrail-конфигом (metric_key, threshold, action), поток событий, превышающий порог. | `done` |
| `B6.1` | `B6-1/B6-2/B6-3/B6-4/B6-5` | Без отчёта по окну времени и в разрезе вариантов нельзя принять взвешенное решение по эксперименту. | `src/application/usecases/reports/get_experiment_report.py`, `src/presentation/rest/routes/reports.py`, доменная агрегация метрик | Вызвать `/experiments/{id}/report?from_time=...&to_time=...` и проверить: фильтрацию по окну, наличие блока `overall`, разрез по вариантам, фиксацию исхода и комментария через `complete` (см. `tests/e2e/test_decide_event_report.py`, `tests/e2e/test_reports.py`). | Эксперимент с запущенным трафиком и валидными событиями, unix-интервалы времени. | `done` |
| `B7.1` | `B7-5/B7-6/B7-7` | Без архитектурных диаграмм сложно быстро понять границы и критичный путь decide → event → report/guardrail. | `docs/architecture/c4_context.md`, `docs/architecture/c4_container.md`, `docs/architecture/c4_component_api.md` | Открыть C4-диаграммы и сопоставить их с кодом и демо-сценарием: акторы/границы системы, контейнеры, компоненты API для критичного пути. | Репозиторий кода и документации, доступ к файлам в `docs/architecture/`. | `done` |
| `B7.2` | `B7-3` | Отсутствие матрицы соответствия затрудняет проверку критериев. | `docs/compliance_matrix_ru.md` | Использовать матрицу как карту: по ID критерия быстро находить реализацию, сценарий проверки и нужные данные. | Доступ к документации и кода. | `done` |
| `B7.3` | `B7-4` | Ключевые архитектурные решения должны быть зафиксированы. | `docs/architecture/decisions_ru.md` | Просмотреть список решений (регистрация admin-only, апрувер-группа, guardrails в процессе, уведомления через Celery, Learnings на OpenSearch и др.) и сверить с кодом/демо. | Документация и код. | `done` |
| `B7.4` | `B7-8` | Карта репозитория для быстрого ориентирования. | `docs/architecture/repo_map_ru.md` | Использовать карту: точки входа, основные папки, где искать критичный поток decide → event → report/guardrail. | Репозиторий кода. | `done` |
| `B7.5` | `B7-9` | Ключевые ограничения и упрощения должны быть явно зафиксированы. | `docs/architecture/limitations_ru.md` | Просмотреть ограничения (первый вход, fallback ревью, Redis/OpenSearch, уведомления, Learnings) и убедиться, что они согласуются с демо и кодом. | Документация и код. | `done` |
| `B8.1` | `B8-1/B8-2/B8-4` | Без негативных и интеграционных тестов легко пропустить регрессы и некорректные edge-cases. | `tests/e2e/test_decide_event_report.py`, `tests/e2e/test_events.py`, `tests/unit/usecases/*.py`, `tests/unit/domain/*.py` | Запустить `uv run pytest -v`, показать наличие негативных и e2e-тестов критичного пути; при необходимости запустить `coverage` и показать отчёт о покрытии. | Окружение для тестов (PostgreSQL, Redis) и подготовленные переменные окружения (см. `docs/runbook_ru.md`). | `done` |
| `B9.1` | `B9-1/B9-2/B9-3/B9-4` | Без health/readiness, метрик и структурированных логов система неготова к эксплуатации. | `src/presentation/rest/app.py` (`/health`, `/ready`, `/metrics`), `src/infra/observability/metrics.py`, `src/infra/observability/logging.py`, `docs/observability_ru.md` | Выполнить `curl` к `/health`, `/ready`, `/metrics`; сгенерировать трафик `/decide` и `/events`; убедиться в росте метрик; проверить формат логов (JSON) и содержимое примеров из документации. | Запущенный API, Prometheus/Grafana (по желанию), нагрузка через сценарии демо. | `done` |
| `B10.1` | `B10-1/B10-2` | Без автоматизированного линтинга и форматирования код быстро деградирует. | `pyproject.toml` (секция `tool.ruff`), `docs/runbook_ru.md`, `docs/observability_ru.md` | Запустить `uv run ruff check .` и `uv run ruff format .`, убедиться в отсутствии ошибок (или в понятных отчётах) и использовании этих команд в CI/локальной разработке. | Репозиторий проекта, установленный `uv` и зависимости dev-группы. | `done` |
